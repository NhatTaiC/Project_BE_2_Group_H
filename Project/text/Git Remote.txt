GitBash:

cd ~/.ssh

ssh-keygen -t rsa -C "username1@mail.com" -f "id_rsa_username1"

ssh-keygen -t rsa -C "username2@mail.com" -f "id_rsa_username2"

pwd

touch config


Host github.com-username1
HostName github.com
User git
IdentityFile ~/.ssh/id_rsa_username1

Host github.com-username2
HostName github.com
User git
IdentityFile ~/.ssh/id_rsa_username2



eval "$(ssh-agent -s)"

ssh-add -D

ssh-add ~/.ssh/id_rsa_username1

ssh-add ~/.ssh/id_rsa_username2

ssh-add -l












CMDER:

Username1:

cd -> thu muc clone git

git config user.name "username1"

git config user.email "username1@mail.com"

git remote set-url origin git@github.com-username1:NhatTaiC/taikhoan1.git


Username2:

cd -> thu muc clone git

git config user.name "username2"

git config user.email "username2@mail.com"

git remote set-url origin git@github.com-username2:flintlookwood/taikhoan2.git